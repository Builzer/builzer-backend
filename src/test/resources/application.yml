jwt:
  expiration:
    access: 1000
    refresh: 1000

oauth:
  github:
    client-id: test
    client-secret: test
    callback-uri: test

feign:
  endpoint:
    github:
      api: http://localhost:${wiremock.server.port}
      oauth: http://localhost:${wiremock.server.port}